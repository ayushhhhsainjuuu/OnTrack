apiVersion: apps/v1
kind: Deployment
metadata:
  name: leave-service
  labels:
    app: leave-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: leave-service
  template:
    metadata:
      labels:
        app: leave-service
    spec:
      imagePullSecrets:
        - name: acr-secret
      containers:
        - name: leave-service
          image: ontrackacr.azurecr.io/leave-service:v1
          ports:
            - containerPort: 4003
          env:
            - name: PORT
              value: "4003"
            - name: SUPABASE_URL
              valueFrom:
                secretKeyRef:
                  name: supabase-secrets
                  key: SUPABASE_URL
            - name: SUPABASE_SERVICE_ROLE_KEY
              valueFrom:
                secretKeyRef:
                  name: supabase-secrets
                  key: SUPABASE_SERVICE_ROLE_KEY
          readinessProbe:
            httpGet:
              path: /health
              port: 4003
            initialDelaySeconds: 5
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: leave-service
spec:
  selector:
    app: leave-service
  ports:
    - protocol: TCP
      port: 80
      targetPort: 4003
  type: ClusterIP
  