apiVersion: apps/v1
kind: Deployment
metadata:
  name: clock-service
  labels:
    app: clock-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: clock-service
  template:
    metadata:
      labels:
        app: clock-service
    spec:
      imagePullSecrets:
        - name: acr-secret
      containers:
        - name: clock-service
          image: ontrackacr.azurecr.io/clock-service:v1
          ports:
            - containerPort: 4002
          env:
            - name: PORT
              value: "4002"
            - name: SUPABASE_URL
              valueFrom:
                secretKeyRef:
                  name: supabase-secrets
                  key: SUPABASE_URL
            - name: SUPABASE_SERVICE_ROLE_KEY
              valueFrom:
                secretKeyRef:
                  name: supabase-secrets
                  key: SUPABASE_SERVICE_ROLE_KEY
          readinessProbe:
            httpGet:
              path: /health
              port: 4002
            initialDelaySeconds: 5
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: clock-service
spec:
  selector:
    app: clock-service
  ports:
    - protocol: TCP
      port: 80
      targetPort: 4002
  type: ClusterIP